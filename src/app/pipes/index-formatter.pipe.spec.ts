import { IndexFormatterPipe } from './index-formatter.pipe';

describe('IndexFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new IndexFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
